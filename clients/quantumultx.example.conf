# Replace <OWNER>/<REPO> and the Gemini policy name before use.

[filter_remote]
https://raw.githubusercontent.com/<OWNER>/<REPO>/main/rules/gemini-core.list, tag=Gemini, force-policy=Gemini, update-interval=86400, opt-parser=false, enabled=true
